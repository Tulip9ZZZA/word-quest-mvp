# Validation Notes — Competency Upgrade

## Completed checks

- `pnpm check` completed without TypeScript errors.
- `pnpm build` completed successfully.
- The live competency workspace opened from the study navigation and exposed the searchable 5,000-word directory, per-word detail view, Oxford reference link, and vocabulary-only CEFR disclaimer.
- A correct Field Test response for `achieve` was recorded and then surfaced as word-level evidence: one attempt, zero misses, a B-tier score, and Field Test attribution.
- Full-page desktop and 390×844 mobile captures were created for the competency workspace. The responsive stylesheet collapses the rail into a menu, stacks scorecards and detail panels, and simplifies directory rows for narrow screens.

## Scope note

Progress is intentionally browser-local in this static MVP. The persistent record model is ready for a future authenticated/cloud-synced implementation.
